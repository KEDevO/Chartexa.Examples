# BTC-USD Tactical View — Standalone HTML

Generated by **Chartexa Pro** dashboard exporter.

Open ``dashboard.html`` directly in a browser.

The page will poll ``/api/dashboard`` every 3000 ms.
Make sure that endpoint is reachable and returns the correct payload
(see data_adapter comments for schema).
